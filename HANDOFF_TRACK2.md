# تسليم المسار الثاني → المسار الأول

> **المسار الثاني مكتمل بالكامل**: الأدوات الثلاث، الـ prompts، الوكلاء الثلاثة،
> node التقرير، الواجهة، والاختبارات الرسمية (46/46 ناجحة) — كلها جاهزة
> ومُختبرة فعلياً، بتواقيع مطابقة لـ `CONTRACTS.md` مع تعديل واحد على شكل
> مخرجات `check_vulnerabilities` (بند ٢). المتبقي مشترك بين الطرفين — بند ١١.

---

## ١. ما اكتمل

### `backend/tools/github_tools.py`
```python
def parse_repo_url(url: str) -> tuple[str, str] | None: ...
def fetch_repo_data(owner: str, repo: str) -> dict: ...          # يرمي RepoNotFound
def open_issue(owner: str, repo: str, title: str, body: str) -> str: ...  # يرمي MissingToken
```
اختبار فعلي على `sl-rwl/test_1`: `full_name`, `open_issues=1`, `readme=12` حرفاً،
`dependency_files=['requirements.txt']`, `code_files=1` (`app.py`). واختبار فشل
فعلي على مستودع غير موجود (`sl-rwl/demo-shop`) رمى `RepoNotFound` بشكل صحيح.

### `backend/tools/osv_tools.py`
```python
def parse_requirements(text: str) -> list[tuple[str, str]]: ...
def check_vulnerabilities(requirements_text: str, limit: int = 10) -> list[dict]: ...  # لا يرمي أبداً
```
اختبار فعلي على `requirements.txt` من `sl-rwl/test_1` (6 حزم قديمة عمداً) — كل
حزمة رجّعت ثغرات حقيقية من OSV.dev، مثال: `django==2.2.0` → `GHSA-2gwj-...` +
٦٦ معرّفاً آخر (انظر بند ٢).

### `backend/tools/secret_tools.py`
```python
def scan_secrets(code_files: dict[str, str]) -> list[dict]: ...
```
اختبار فعلي مزدوج: `app.py` الحقيقي من `sl-rwl/test_1` → لا كشف (نظيف فعلاً)،
وملف محلي بمفاتيح وهمية بكل الأنماط الخمسة (GitHub/OpenAI/AWS/Google/عام) →
**8/8** اكتُشفت بالنوع الصحيح والتقنيع الصحيح، بلا أي إشارة كاذبة.

---

## ٢. تعديل على العقود — `check_vulnerabilities`

**قبل:** `{"package", "version", "vuln_ids", "summary"}` — كل المعرّفات.
**بعد:**
```python
{"package": str, "version": str, "vuln_ids": list[str], "total_count": int, "summary": str}
```
`vuln_ids` محدودة بأول **5** معرّفات، `total_count` هو العدد الحقيقي، و`summary`
أصبح ملخص أول ثغرة فقط بدل دمج كل الملخصات.

**السبب:** `django==2.2.0` وحده رجّع **71** معرّف ثغرة. تمرير هذا الحجم لكل حزمة
قديمة عبر `findings` إلى الـ LLM في node التقرير يُضخّم الـ state والتكلفة بلا
فائدة إضافية — 5 أمثلة + عدد كلي كافٍ كدليل. مُحدَّث في `CONTRACTS.md`.

---

## ٣. تنبيه — استثناءات `fetch_repo_data` غير `RepoNotFound`

الدالة تستخدم `resp.raise_for_status()` داخلياً، فقد ترمي `requests.HTTPError`
عاماً في حالات غير `RepoNotFound` (404 فقط): **403** عند تجاوز حد استعلامات
GitHub، أو **500/502** عند عطل مؤقت في GitHub نفسه. تحقّقتُ من `fetch.py` —
`fetch_node` يمسكها بالفعل عبر `except Exception` عام بعد `except RepoNotFound`
(رسالة `tool_error` منفصلة عن `not_found`)، فلا حاجة لتعديل حالياً. **فقط
تنبيه لأي تعديل مستقبلي على node الجلب:** لا يُضيَّق هذا الـ catch إلى
`RepoNotFound` وحدها، وإلا ستُسقِط هذه الحالات الـ graph بدل رسالة مفهومة.

---

## ٤. قاعدة الكتابة (بعد حادثة Flask)

**`open_issue` لا تُستدعى إلا على مستودعات نملكها (`sl-rwl/*`).** أي مستودع
خارجي = قراءة فقط عبر `fetch_repo_data`، أبداً كتابة. السبب: سكربت اختبار
استخدم مستودع Flask الحقيقي كسيناريو "نجاح" مع موافقة تلقائية ففتح بلاغاً
فعلياً هناك (أُغلق لاحقاً من قِبل قائمي المشروع، لا يمكن حذفه). أي كود أو
سكربت اختبار يستدعي `open_issue` أو يُشغّل الـ graph حتى `publish` مع
`approved=True` يجب أن يستهدف `sl-rwl/*` حصراً، أو يحاكي الاستدعاء (mock).

---

## ٥. ملف الاختبار المحلي غير المرفوع

`scripts/fixtures/fake_secrets_sample.py` يحتوي مفاتيح وهمية بصيغ حقيقية
(لإثبات أن `scan_secrets` يكتشف شيئاً، لأن `app.py` الحقيقي نظيف). **غير مرفوع
لـ git** (مُضاف لـ `.gitignore`) لأن صيغه تطابق ما يبحث عنه GitHub push
protection حرفياً — رفعه قد يُحظر. لإعادة إنشائه محلياً:
```bash
python scripts/generate_fake_secrets_fixture.py
```
السكربت نفسه آمن للرفع: كل مفتاح مبني من (بادئة + جسم) في سطرين منفصلين،
فلا يوجد سطر واحد فيه يطابق نمط سرّ كامل.

---

## ٦. `backend/prompts.py`

دالة `*_system_prompt(language)` واحدة لكل من الوكلاء الثلاثة + التقرير، كلها
مبنية على قاعدتين مشتركتين تُركَّبان في كل prompt:

- **`LANGUAGE_RULE`**: النص بلغة `state["language"]`، مع بقاء المعرّفات
  التقنية (أسماء الحزم، CVE/GHSA، أرقام البلاغات `#12`، مسارات الملفات، أنواع
  الأسرار) إنجليزية دائماً بغض النظر عن لغة النص.
- **`GUARDRAILS`**: تمنع اختراع نتائج غير مدعومة بالمدخلات، تُلزم بإرجاع
  نتيجة `"none"` صراحة بدل تلفيق مشكلة، **وتحمي من prompt injection**: أي
  تعليمات مضمّنة داخل محتوى المستودع نفسه (نص README، عناوين/محتوى
  البلاغات، تعليقات الكود) تُعامَل كبيانات للتحليل فقط ولا تُنفَّذ أبداً كأوامر.

مُتحقَّق فعلياً في كل الاختبارات أدناه: المعرّفات بقيت إنجليزية في كل تشغيل
عربي، ولا توجد حالة اختُرعت فيها نتيجة غير مدعومة بالمدخلات.

---

## ٧. الوكلاء الثلاثة — `backend/graph/agents.py`

**فلسفة التصميم موحّدة عبر الثلاثة** (نفس مبدأ `supervisor.py`): الكود يحسب
كل ما يمكن حسمه بيقين (الحقائق، الترتيب، البنية)، والـ LLM يُستخدم فقط فيما
يحتاج فهماً لغوياً فعلياً (الصياغة، أو حكماً دلالياً لا بديل له). كل وكيل
يسقط حتمياً بلا LLM عند أي فشل (مفتاح ناقص، شبكة، رد غير صالح) — لا يُسقط
الـ graph أبداً.

### `security_agent`
- `check_vulnerabilities` + `scan_secrets` يبنيان قائمة عناصر ثابتة
  (`severity`, `evidence` من مخرجات الأداة مباشرة، لا اجتهاد). الـ LLM يكتب
  فقط `title`/`detail` لكل عنصر، بنفس العدد والترتيب — أي اختلاف في العدد
  يُسقط النتيجة كاملة لصياغة حتمية بديلة.
- **اختبار فعلي**: تشغيلتان متتاليتان على `sl-rwl/test_1` (6 حزم قديمة عمداً)
  أعطتا **6/6** نفس الحزم بنفس `evidence` (معرّفات GHSA) بنفس الترتيب في
  الحالتين — العدد والبنية حتميان فعلاً، فقط صياغة النص اختلفت قليلاً بين
  التشغيلتين (متوقَّع من LLM).

### `issues_agent`
- **حتمي بالكامل بلا LLM**: `_stale_issues()` — بلاغ مهمل = `comments == 0`
  و`created_at` أقدم من 90 يوماً (تاريخ فعلي، لا تقدير من النموذج).
- **يحتاج LLM حصراً**: مجموعات التكرار الدلالي (`duplicate_groups`) وأهم 3
  بالأولوية (`priority`) — لا شيء غير ذلك يُطلب من النموذج.
- **اختبار فعلي**: بلاغات `sl-rwl/test_1` الحقيقية بلاغ واحد فقط (غير كافٍ
  لاختبار التكرار/الإهمال)، فأضفتُ سيناريو مصطنعاً (5 بلاغات) أثبت الآلية
  فعلياً: بلاغان بصياغتين مختلفتين تماماً (إنجليزي/عربي، لا كلمات مشتركة)
  اكتُشفا كتكرار واحد بالمعنى ✓، بلاغ عمره 200 يوم بلا تعليقات اكتُشف مهملاً
  حتمياً ✓، وبلاغ "تجاوز مصادقة" صُنِّف أول أولوية فوق بلاغ تعطّل وبلاغ واجهة
  بسيط ✓ — في كلا اللغتين.

### `docs_agent`
- README فارغ فعلاً (`.strip()` فارغ) = **مسار حتمي بلا LLM إطلاقاً**، finding
  واحد `severity="critical"`.
- README موجود (حتى لو ناقص) = 4 معايير ثابتة بالكود فقط
  (`what_it_is`/`installation`/`usage`/`license`)، الـ LLM يقرر `met: bool` +
  شرح قصير لكل واحد بنفس العدد والترتيب — لا يضيف ولا يحذف معياراً. قيد
  صريح في الـ prompt يمنع اقتراح أي قسم خارج الأربعة (لا Contributing ولا
  Troubleshooting ولا badges).
- **اختبار فعلي**: `sl-rwl/test_1` (README = 12 حرفاً، غير فارغ) → **4/4**
  findings ناقصة (`severity="low"` لكل معيار). `sl-rwl/test_clean` (README =
  577 حرفاً، وصف + تثبيت + تشغيل + رخصة MIT) → finding واحد فقط "التوثيق
  مكتمل"، **بلا أي اقتراح إضافي** — تحقَّق القيد فعلياً.

---

## ٨. node التقرير — `backend/graph/report.py`

- **ترتيب حتمي بالكامل بالكود قبل أي LLM**: `sorted(findings, key=severity_rank)`
  (`critical > high > medium > low > none`)، وأخطر 3 (`top_issues`) محسوبة في
  الكود. النموذج لا يرى هذا القرار، فقط findings مُرتَّبة مسبقاً.
- دور الـ LLM محدود بدقة لشيئين فقط: `executive_summary` (جملتان) و
  `recommendations` — البنية والعناوين والأدلة كلها من الكود مباشرة من
  findings الموجودة أصلاً (لا إعادة توليد لها).
- **لا يفترض عمل الوكلاء الثلاثة**: قسم "Findings by Area" يتخطى أي وكيل
  غير موجود في `agents_done`.
- **كل findings بـ `severity="none"`**: **لا يُستدعى LLM إطلاقاً** — نص ثابت
  حتمي يقول صراحة إن المستودع سليم، حماية إضافية ضد التلفيق.
- `issue_body = report` حرفياً، `issue_title` قصير: `"Repo health report — owner/repo"`.
- **اختبار فعلي**: سلسلة كاملة (`security_agent` → `issues_agent` →
  `docs_agent` → `report_node`) على مستودعين باللغتين:
  - `sl-rwl/test_1` (غير سليم): 11 finding، أخطر 3 = الثغرات الثلاث الأولى
    بالضبط، ثابتة في كلا اللغتين، ملخص وتوصيات مبنيان فعلاً على النتائج.
  - `sl-rwl/test_clean` (سليم): 3 findings كلها `none` → قسم "أخطر المشاكل"
    عرض "لم يُعثر على أي مشكلة فعلية" بدل قائمة، والملخص/التوصيات هما النص
    الثابت المبرمَج حرفياً (تأكيد فعلي أن مسار السلامة تخطّى LLM تماماً).

---

## ٩. الواجهة — `ui/app.py`

- **HTTP فقط، لا استيراد للـ graph إطلاقاً** (شرط إلزامي من البريف) — الملف
  كاملاً يستورد `requests`/`streamlit`/`dotenv` فقط، ينادي `POST /analyze`
  و`POST /approve` حسب عقد `CONTRACTS.md` حرفياً.
- ثنائية اللغة كاملة: مبدّل مضغوط (`selectbox` في عمود ضيق أعلى الصفحة)
  يبدّل نصوص الواجهة **و**`language` المُرسَل في الطلب معاً، ومع العربية
  **تتحول الواجهة فعلياً لـ RTL** (CSS مُحقن `direction: rtl` — تحقّقتُ
  بصرياً: زر "افحص" وحقل الإدخال انتقلا لليمين تلقائياً عند التبديل).
- تدفق كامل: حقل الرابط + زر الفحص → عرض التقرير بـ `st.markdown()` → عند
  `awaiting_approval` قسم موافقة بشرية بزرين ("نعم، افتح البلاغ" / "لا،
  اكتفِ بالتقرير") → عند الموافقة يعرض `issue_url` الحقيقي المُرجَع من
  الخادم. عند `rejected` لا يظهر قسم الموافقة إطلاقاً.
- **اختبار فعلي في المتصفح** (لا وصف نظري): شغّلت `streamlit run ui/app.py`
  مقابل الخادم الوهمي وجرّبت 4 سيناريوهات كاملة — نجاح+موافقة (رابط بلاغ
  ظهر فعلياً)، نجاح+رفض النشر ("لم يُفتح أي بلاغ")، رفض الطلب (رابط غير
  صالح، بلا زري موافقة)، وتبديل اللغة en↔ar مع RTL. تأكّدتُ من سجلّات
  الخادم الوهمي أن `POST /analyze` و`POST /approve` وصلا فعلياً عبر الشبكة
  (200 OK)، لا استدعاء مباشر لأي كود من الـ graph.

### `scripts/mock_backend.py` — أداة تطوير مؤقتة
FastAPI صغير يرجع ردود ثابتة بشكل عقد `CONTRACTS.md` (`/health`, `/analyze`,
`/approve`) بلغتين، بلا أي علاقة بالـ graph. التبديل للخادم الحقيقي لاحقاً =
تغيير `FAHES_BACKEND` في `.env` فقط (أضفته لـ `.env.example`)، بلا لمس كود
الواجهة. **يُحذف عند نقطة الالتقاء الثانية** — انظر بند ١١.

---

## ١٠. الاختبارات الرسمية — `tests/`

**46/46 اختباراً ناجحة**، تعمل **بلا مفاتيح وبلا شبكة** — كل استدعاء خارجي
(`requests.get/post`, `get_llm`) مموَّه بـ `unittest.mock`/`monkeypatch`.
تحقّقتُ من هذا فعلياً بتشغيل الحزمة كاملة مرتين: مرة عادية، ومرة مع حذف
`OPENAI_API_KEY`/`OPENROUTER_API_KEY`/`ANTHROPIC_API_KEY`/`GOOGLE_API_KEY`/
`GITHUB_TOKEN`/`LLM_PROVIDER` من البيئة — نفس النتيجة 46/46 في الحالتين.
المخرجات الكاملة محفوظة في `tests/proof_of_execution.txt`.

| الملف | يغطي |
|---|---|
| `test_github_tools.py` | 8 صيغ رابط صالحة + 6 غير صالحة، `RepoNotFound` عند 404، `MissingToken` بلا توكن (مع تأكيد عدم الوصول للشبكة أصلاً) |
| `test_osv_tools.py` | `parse_requirements` يتجاهل `>=`/`~=`/`-e`/`git+`/التعليقات؛ `check_vulnerabilities` يسجّل `error` بدل رمي استثناء، وفشل حزمة واحدة لا يوقف الباقي |
| `test_secret_tools.py` | الأنماط الأربعة (GitHub/OpenAI/AWS/Google) + العام، التقنيع الصحيح، صفر إنذارات كاذبة |
| `test_agents.py` | لكل وكيل: `agents_done` بعنصر واحد باسمه، شكل finding كامل، مسار نجاح LLM (مموَّه) **ومسار فشله** (السقوط الحتمي) |
| `test_report.py` | الترتيب الحتمي بالخطورة قبل أي LLM، حالة "الكل none" وحالة القائمة الفارغة تعطيان تقريراً سليماً **بلا استدعاء LLM إطلاقاً** (assertion صريح يفشل لو استُدعي) |

---

## ١١. ما تبقّى — مشترك بين الطرفين

**المسار الثاني اكتمل بالكامل.** المتبقي عمل مشترك عند نقاط الالتقاء
(حسب `WORK_SPLIT.md`):

1. **قائمة الحذف عند نقطة الالتقاء الأولى/الثانية** — أدوات تطوير فقط،
   البريف يمنع أي أداة/رد ترجع بيانات معلّبة في النسخة النهائية:
   - `backend/stubs.py` بالكامل
   - `scripts/mock_backend.py` بالكامل
   - أي كتلة `try/except ImportError` مؤقتة تبقّت في `backend/graph/*.py`
     (كانت موسومة `# ── مؤقت: يُحذف عند نقطة الالتقاء الأولى ──`)
2. **بناء الـ backend الحقيقي (FastAPI)** — يربط `build_graph()` الجاهز
   بعقد `/analyze` و`/approve`، ويستبدل `scripts/mock_backend.py`.
3. **تشغيل السلسلة كاملة end-to-end** — الواجهة الحقيقية ← الـ backend
   الحقيقي ← الـ graph الحقيقي بأدوات ووكلاء حقيقيين، بلا أي طرف وهمي.
4. **README الكامل** + **السلايدات** (بعد اكتمال النظام، حسب `WORK_SPLIT.md`).
