# فكرة المشروع — GitHub Repo Health Agent

## المشكلة

المستودعات التقنية النشطة تتراكم فيها الفوضى بصمت، ولا أحد يخصص وقتاً للتوقف ومراجعتها شاملة:

- **ثغرات في الاعتماديات**: حزم بإصدارات قديمة فيها ثغرات معروفة (CVE/GHSA) تبقى بلا اكتشاف حتى وقوع حادثة.
- **مفاتيح مكشوفة**: مفاتيح API أو أسرار تُدفع بالخطأ إلى الكود وتبقى ظاهرة في التاريخ أو الفروع.
- **بلاغات متكدسة (issues)**: مئات البلاغات المفتوحة، بعضها مكرر بلغتين مختلفتين، بعضها مهمل منذ شهور، بدون أي أولوية واضحة.
- **توثيق متهالك**: ملف README لا يشرح ما هو المشروع، ولا كيف يُثبَّت، ولا كيف يُشغَّل، وأحياناً بلا ترخيص واضح.

الفرق الصغيرة لا تملك الوقت ولا الشخص المخصص لمراجعة هذا دورياً، فتتراكم المخاطر حتى تتحول لحادثة أمنية أو لتجربة سيئة لمساهم جديد.

## الحل

نظام وكلاء متعدد (multi-agent) مبني على LangGraph يفحص مستودع GitHub بشكل شامل عند الطلب:

1. **حارس (Guardrail)**: يتحقق من صلاحية رابط المستودع ويرفض بأدب أي طلب غير سليم.
2. **جلب (Fetch)**: يسحب بيانات المستودع الحقيقية (README، الاعتماديات، البلاغات المفتوحة، ملفات الكود).
3. **مشرف (Supervisor)**: يقرر ديناميكياً أي وكيل يعمل تالياً بناءً على حالة المستودع والوكلاء المنفَّذين سابقاً.
4. **وكيل الأمن**: يفحص الاعتماديات عبر OSV.dev بحثاً عن ثغرات معروفة، ويفحص الكود بحثاً عن مفاتيح مكشوفة.
5. **وكيل البلاغات**: يحلل الـ issues المفتوحة — يكشف المكرر (حتى لو بلغتين مختلفتين)، المهمل، ويقترح أولويات.
6. **وكيل التوثيق**: يقيّم README مقابل أربعة معايير: هل يوضح ما هو المشروع، خطوات التثبيت، خطوات التشغيل، والترخيص.
7. **تقرير + موافقة بشرية (Human-in-the-loop)**: يجمّع النتائج المتراكمة من كل الوكلاء في تقرير Markdown بلغة المستخدم، ويتوقف بانتظار موافقة بشرية صريحة قبل فتح أي بلاغ (issue) فعلي في المستودع.

## لماذا وكيل، وليس سكربت ثابت؟

سكربت ثابت بمسار واحد لا يكفي لأن كل عملية فحص تتطلب **قرارات متسلسلة غير معروفة مسبقاً**:

- عدد الوكلاء اللازمين وترتيب عملهم يعتمد على حالة المستودع نفسه — مستودع بلا ملف اعتماديات لا يحتاج فحص أمني بنفس الشكل، ومستودع بلا README شبه فارغ يحتاج مساراً مختلفاً عن مستودع موثّق جزئياً.
- **المشرف (Supervisor)** يقرأ نتائج كل وكيل قبل أن يقرر التالي — وهذا تفريع (branching) حقيقي لا يمكن تشفيره كخطوات ثابتة في برومبت واحد.
- كشف البلاغات المكررة عبر لغتين، وربط ثغرة برقم CVE/GHSA بسياقها، يتطلب استدلالاً (reasoning) وليس مطابقة نصية بسيطة.
- **فتح بلاغ فعلي في مستودع طرف ثالث فعل له عواقب حقيقية** — لذلك لا يجوز أن يُتخذ تلقائياً؛ الوكيل يتوقف وينتظر موافقة بشرية صريحة، وهذا سلوك تفاعلي (interrupt) لا يقدر عليه سكربت خطي أو نموذج بلا حالة (state).

باختصار: لو كان المسار ثابتاً معروفاً مسبقاً، لكفى سكربت. لكن "أي وكيل يعمل تالياً، وهل يستحق المستودع فتح بلاغ فعلاً" قرار يعتمد على البيانات نفسها في كل مرة.

## الفئة المستهدفة

**الفرق التقنية الصغيرة (2–10 مطورين)** التي تدير مستودع أو أكثر على GitHub بدون فريق أمني أو DevOps مخصص:

- الدافعون: قائد الفريق التقني أو مالك الشركة الناشئة.
- المستخدمون: المطورون أنفسهم، الذين يستلمون التقرير ويوافقون على الإجراءات.

هذه الفرق تعرف أن لديها مخاطر متراكمة، لكنها لا تملك الوقت لمراجعة كل مستودع يدوياً كل أسبوع.

## نموذج العمل

- **التسعير**: اشتراك شهري بحوالي **300 ريال سعودي لكل فريق/مستودع** (يشمل عدداً محدوداً من عمليات الفحص شهرياً).
- **الإيراد**: عدد الفرق المشتركة × السعر الشهري = الإيراد الشهري المتوقع.
- **التكلفة**: تكلفة كل عملية فحص = استدعاءات LLM (المشرف + ثلاثة وكلاء + صياغة التقرير) + استعلامات OSV.dev + استدعاءات GitHub API. عملية الفحص متعددة الأدوار (multi-turn) أغلى من استدعاء واحد، لكنها أرخص بكثير من ساعة عمل مطور بشري لمراجعة يدوية مماثلة.
- **الاحتفاظ بالعملاء (Retention)**: بمجرد أن يعتاد الفريق على تقارير دورية موثوقة، التوقف عنها يعني العودة للمراجعة اليدوية العشوائية — وهذا يرفع تكلفة التحول (switching cost).
